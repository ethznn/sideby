# README 0.6.0 Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update both READMEs and the primary preview screenshot so they accurately present Sideby 0.6.0.

**Architecture:** Keep the shared image paths stable, replace only the stale menu-panel image, and make equivalent targeted copy edits in the English and Korean READMEs. Validate text parity, image integrity, and staging scope without touching application code or Direct Space Jump research.

**Tech Stack:** Markdown, PNG, macOS Sideby 0.6.0, `sips`, Git

## Global Constraints

- Keep `docs/images/sideby-swipe-onboarding.png` unchanged.
- Keep the existing preview image paths and displayed widths.
- Use an actual Sideby 0.6.0 menu-panel capture; do not synthesize or edit an outdated UI into a new screenshot.
- Do not include private, diagnostic, or Direct Space Jump research data.
- Preserve all unrelated uncommitted workspace changes.

---

### Task 1: English and Korean README Copy

**Files:**
- Modify: `README.md`
- Modify: `README.ko.md`

**Interfaces:**
- Produces: English and Korean 0.6.0 status, feature, alt-text, and Quick Start copy with equivalent meaning

- [ ] **Step 1: Update the English preview alt text and Status paragraph**

Use this alt text:

```html
alt="Sideby 0.6.0 menu showing pinned controls and a compact Context matrix"
```

Replace the first Status paragraph with:

```markdown
Sideby is pre-1.0 software. Version 0.6.0 keeps the app in the menu bar without a Dock icon and makes the master Sideby toggle authoritative for direct Context moves. The Context matrix now labels its horizontal and vertical axes and sizes its header to its content, so the first Space row starts closer to the Context controls without losing alignment.
```

- [ ] **Step 2: Update the English feature bullets and Quick Start guidance**

Use these feature claims:

```markdown
- Menu bar-only app with a resizable settings popover for quick control across multiple displays, including pinned Sideby/Switch controls above the scrolling setup content; Sideby does not remain in the Dock.
- Compact Context matrix for reviewing which displays belong to each Context, including directional Context/Display axes, naturally sized headers, each display's captured Space number, user-defined display row order, draggable Space membership, and a resizable display-name column.
- Direct Context activation from the matrix for jumping to a named Context; activation is unavailable while Sideby is off, switching, or capturing.
```

Add after the local-build command block:

```markdown
Sideby stays in the menu bar and does not show a Dock icon. Use the Sideby menu bar item to reopen the controls.
```

- [ ] **Step 3: Apply semantically equivalent Korean copy**

Use this alt text:

```html
alt="고정 컨트롤과 컴팩트 Context matrix를 보여주는 Sideby 0.6.0 메뉴"
```

Replace the first Status paragraph with:

```markdown
Sideby는 pre-1.0 소프트웨어입니다. 0.6.0부터 앱은 Dock 아이콘 없이 메뉴바에만 머물고, 최상단 Sideby 토글이 Context 직접 이동에도 동일하게 적용됩니다. Context matrix는 가로/세로 축을 표시하고 내용에 맞춰 헤더 높이를 정해, 정렬을 유지하면서 첫 Space 행의 불필요한 여백을 줄였습니다.
```

Use these feature claims:

```markdown
- 스크롤되는 설정 콘텐츠 위에 고정된 Sideby/Switch 컨트롤을 포함해 여러 디스플레이를 빠르게 제어하는 리사이즈 가능한 메뉴바 전용 설정 팝오버. Sideby는 Dock에 남지 않습니다.
- 각 Context에 속한 디스플레이, Context/디스플레이 방향 축, 내용에 맞춰지는 헤더, 캡처된 Space 번호, 사용자가 정한 디스플레이 행 순서, 드래그 가능한 Space membership, 조절 가능한 디스플레이 이름 열을 확인하는 컴팩트 Context matrix
- Context matrix에서 이름 있는 Context로 바로 이동하는 기능. Sideby가 꺼져 있거나 전환 또는 캡처 중이면 이동할 수 없습니다.
```

Add after the local-build command block:

```markdown
Sideby는 Dock 아이콘을 표시하지 않고 메뉴바에만 머뭅니다. 컨트롤을 다시 열려면 메뉴바의 Sideby 항목을 사용하세요.
```

- [ ] **Step 4: Validate bilingual text parity**

Run:

```bash
rg -n "0\.5\.0|0\.6\.0|Dock|Context matrix|Context 매트릭스|Context matrix" README.md README.ko.md
```

Expected: no current-status reference to 0.5.0; both files mention 0.6.0, Dockless presentation, matrix behavior, and guarded direct activation.

---

### Task 2: Current Menu-Panel Screenshot

**Files:**
- Modify: `docs/images/sideby-context-capture.png`
- Preserve: `docs/images/sideby-swipe-onboarding.png`

**Interfaces:**
- Consumes: running `dist/Sideby.app` version 0.6.0
- Produces: current tightly cropped PNG at the existing Context preview path

- [ ] **Step 1: Open the current Sideby 0.6.0 menu panel**

Confirm the bundle version first:

```bash
/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' dist/Sideby.app/Contents/Info.plist
```

Expected: `0.6.0`.

Open the Sideby menu-bar item and leave the floating panel visible with the Context section in view.

- [ ] **Step 2: Capture the actual floating panel**

Capture the Sideby panel at native resolution and crop to the panel bounds. The image must show:

- pinned Sideby/Switch controls;
- no last-switch row;
- `Contexts →` / `컨텍스트 →` and `Displays ↓` / `디스플레이 ↓`;
- the naturally sized compact header and aligned first Space row.

Save the PNG at `docs/images/sideby-context-capture.png` without changing the path.

- [ ] **Step 3: Inspect image integrity and content**

Run:

```bash
sips -g pixelWidth -g pixelHeight docs/images/sideby-context-capture.png
file docs/images/sideby-context-capture.png
```

Inspect the full-resolution image and confirm the UI is current, text is readable, cropping is tight, and no private or diagnostic data appears.

---

### Task 3: Documentation Verification and Commit

**Files:**
- Modify: `README.md`
- Modify: `README.ko.md`
- Modify: `docs/images/sideby-context-capture.png`
- Create: `docs/superpowers/plans/2026-07-12-readme-0.6.0-refresh.md`

**Interfaces:**
- Consumes: completed README copy and screenshot
- Produces: focused documentation commit on `codex/readme-0.6.0-refresh`

- [ ] **Step 1: Verify paths, unchanged onboarding image, and patch formatting**

Run:

```bash
test -f docs/images/sideby-context-capture.png
test -f docs/images/sideby-swipe-onboarding.png
git diff --check
git diff -- README.md README.ko.md
git diff --stat -- docs/images/sideby-context-capture.png docs/images/sideby-swipe-onboarding.png
```

Expected: both paths exist, no whitespace errors, bilingual changes are aligned, the Context image changed, and the onboarding image did not.

- [ ] **Step 2: Stage only README-refresh files**

```bash
git add README.md README.ko.md docs/images/sideby-context-capture.png docs/superpowers/plans/2026-07-12-readme-0.6.0-refresh.md
```

Confirm `git diff --cached --name-only` lists only those four files.

- [ ] **Step 3: Commit the implementation**

```bash
git commit -m "docs: refresh README for 0.6.0"
```
